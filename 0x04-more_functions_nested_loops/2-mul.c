#include "main.h"
#include <stdio.h>
/**
 * mul -  function that multiplies two integers
 * @a: is in ASCII table
 * @b: is in ASCII table
 *
 * Return: Addition of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
	printf("\n");
}
